source "https://rubygems.org"

# GitHub Pages 会自动使用 github-pages gem，包含所有支持的插件
# 本地开发时可以使用以下配置
# gem "github-pages", group: :jekyll_plugins

# Windows 不支持 tzinfo-data
platforms :mingw, :x64_mingw, :mswin, :jruby do
  gem "tzinfo", ">= 1", "< 3"
  gem "tzinfo-data"
end

